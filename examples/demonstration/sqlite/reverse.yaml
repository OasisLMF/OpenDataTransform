mapping:
  options:
    input_format: B
    output_format: A
extractor:
  path: converter.connector.SQLiteConnector
  options:
    database: sqlite.db
    select_statement: select_rev.sql
    insert_statement: insert_rev.sql
loader:
  path: converter.connector.SQLiteConnector
  options:
    database: sqlite.db
    select_statement: select_rev.sql
    insert_statement: insert_rev.sql

mapping:
  options:
    input_format: B
    output_format: A
extractor:
  path: converter.connector.SQLiteConnector
  options:
    path: sqlite.db
    select_statement: select_rev.txt
    insert_statement: insert_rev.txt
loader:
  path: converter.connector.SQLiteConnector
  options:
    path: sqlite.db
    select_statement: select_rev.txt
    insert_statement: insert_rev.txt

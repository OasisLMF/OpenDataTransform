mapping:
  options:
    input_format: A
    output_format: B
extractor:
  path: converter.connector.PostgresConnector
  options:
    host: localhost
    port: 5432
    database: postgres
    user: postgres
    password: password
    select_statement: select.sql
    insert_statement: insert.sql
loader:
  path: converter.connector.PostgresConnector
  options:
    host: localhost
    port: 5432
    database: postgres
    user: postgres
    password: password
    select_statement: select.sql
    insert_statement: insert.sql
